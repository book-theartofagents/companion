# Playground

This folder contains quick experimentation environments.

- `ch1-demo.ipynb` — Jupyter notebook testing Chapter 1 spec
- `mock-agent.py` — Minimal agent skeleton

Use these to prototype before committing to a full implementation.

> Run `pip install -r ../requirements.txt` first.